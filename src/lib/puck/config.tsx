// This module simply re-exports the compiled Puck configuration used across the
// editor and published pages.  The actual configuration lives in
// config.fixed.tsx where all components, fields and behaviour are defined.
export { puckConfig } from "./config.fixed";